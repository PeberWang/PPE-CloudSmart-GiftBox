# Session 2026-06-19-10

跨日延续 2026-06-18-22 session。**用户彻底调整方向**：放弃渐进迁移，改为一次性彻底重构。本 session 同时完成大规模代码重构 + README 普适化 + 文科生视角部署体验打磨。

## 关键决策（用户敲定）

### 架构层（早上）
1. **完全放弃 db**：bitable 是唯一真相源，data/db/ 目录 + 相关代码完全删除
2. **bitable 清空重建**：从零开始
3. **完整链路跑通**：本地资料 → 飞书 drive → OSS → OCR → 摘要存飞书 drive → 课程文档（insights/recommendation 初始可空）
4. **双层门控**：学生表单提交主数据表里没有的新课，sync 跳过 + 警告，等管理员在 bitable 加这门课记录后重跑 sync 才走文档流程
5. **用户自己执行命令**：我写代码 + README，用户在终端跑
6. **多格式 OCR**：raw 资料可能含 Excel/Word/PDF，Word/Excel/PPT 转 PDF 再 OCR（LibreOffice headless）
7. **raw 课程清单**（如 `实测启用时课程录入-20260619.xlsx`）：管理员首次录入已知课程到主数据表

### 部署体验层（下午 — 文科生视角 review）
8. **README 普适性**：任意院校任意专业的文科生能跟着启用和维护知识库
9. **前置说明要详细**：每个准备工作（装 Python / 装 LibreOffice / 注册 4 个云账号）都要有截图级菜单路径说明，不能假设用户会
10. **虚拟环境隔离**：setup.bat 创建 venv，不污染系统 Python；start.bat 双击打开已激活 venv 的终端
11. **Python 检测不假设**：先看用户有没有装 Python，再判断版本够不够新，分情况提示
12. **Excel 枚举值要说清**：每个字段（学期/类型/考核方式）合法值清单 + 「直接填文字不要下拉」
13. **wiki 权限问题**：wiki 命令建的知识库默认应用是 owner，真实管理员无权编辑，必须跑 grant-wiki
14. **手机号注册支持**：飞书 wiki API 不支持 email/mobile，命令内部用 contact API 转 openid
15. **保存 URL 到飞书群**：bitable URL + wiki URL 都发群置顶，否则管理员找不到资源

### 下一阶段目标（用户敲定，记入 production-checklist）
16. **枚举自定义**：移到 yaml 配置文件，用户改配置不改代码
17. **摘要 map-reduce 分块**：≤1000 字 + 长文档分块摘要
18. **相似度检测 + 合并命令**：LLM 串讲合并相似资料 + 管理员一行命令合并
19. **项目架构整理 + 上云文件审视**：代码结构、CLI 命令组织、文档结构都有斟酌空间；OSS 上云的文件可能有冗余

## 执行进程

### 上午：大规模重构
- [x] 第一次 plan（OSS 修补 + 渐进迁移）→ 用户拒绝，调整方向
- [x] 第二次 plan（彻底重构）→ 用户批准
- [x] **WP4 OSS 直链修复**：oss.py download_url 支持纯直链 + settings 加 oss_public_base + archive_service 修文件链接 text bug
- [x] **WP1 删 db + CourseDataService 重写**：删 data/db/ + scripts/seed_course_db.py + course_schema.py 删 _load_year 等 + data_adapter 删 read_index 等 + CourseDataService 重写读 bitable 三张表
- [x] **WP2 sync 重写 + 双层门控**：sync_service 完整重写 + CourseDataService.get_all 内置门控位置 2
- [x] **WP3 新增 4 个 CLI**：reset-bitable / seed-course / seed-materials / ocr-materials（多格式 + LibreOffice + zip 解压递归）
- [x] **WP5 README + 5 篇系列文档**：00 快速开始 / 01 管理员 / 02 学生 / 03 故障排查 / 04 技术原理
- [x] Commit `f9c65cc`（WP1-WP4 重构 16 files +1286 -929）
- [x] Commit `727152b`（WP5 README + 5 篇系列文档 +1659 -105）

### 下午：文科生视角 review 迭代
- [x] README/00 文科生视角重写：补 Python/LibreOffice/OSS ACL 详细步骤（commit `e61f962`）
- [x] 加 setup.bat / setup.sh 一键脚本（自动装 LibreOffice + Python 依赖 + .env）（commit `602aea2`）
- [x] 加虚拟环境（venv）+ start.bat / start.sh（不污染系统 Python）（commit `e061bb0`）
- [x] Python 版本检测改进（先查装没装，再查版本是否 >= 3.10）（commit `c99cad4`）
- [x] Excel 枚举值说明 + grant-wiki 命令 + 保存 URL 提示（commit `a219d6b`）
- [x] grant-wiki/grant-bitable 支持手机号（contact API 转 openid）+ 新增 open-wiki 命令（commit `982597e`）

### 当前 git 状态
- 工作树干净
- origin/master 最新 commit `982597e`
- 今日 7 个 commit 全部 push 完成
- 21 个 CLI 命令全部 import OK + 注册成功

## 实测流程（用户跑）

按 README + 00-快速开始.md 流程，分 7 阶段：

```bash
# 阶段 0：环境准备（约 30-60 分钟）
# - 装 Python 3.10+
# - git clone 项目
# - 双击 setup.bat（装 venv + LibreOffice + 依赖 + .env）
# - 注册 4 个云账号：飞书 / 阿里云 OSS / DeepSeek / 智谱 GLM
# - 填 .env
# - 阿里云控制台 OSS bucket ACL 改公共读

# 阶段 1：首次部署（双击 start.bat 后在激活的终端里跑）
python deploy.py init-bitable              # 建 bitable 三张表
python deploy.py open-bitable              # bitable 凭链接编辑
python deploy.py seed-course --from-file data/课程清单.xlsx
python deploy.py wiki                       # 建知识库 + 4 个学年文档
python deploy.py grant-wiki 你的邮箱        # 重要！否则 UI 改不了
python deploy.py docs                       # 生成课程文档
python deploy.py link                       # 回填导航表链接

# 阶段 2：实测概率论（用户扮管理员）
python deploy.py reset-bitable --yes
python deploy.py seed-course --from-file data/实测启用时课程录入-20260619.xlsx
python deploy.py seed-materials "data/courses/大二上/概率论与数理统计（刘会刚老师）" 概率论与数理统计 --grade 22级 --reason "初始录入测试资料"
python deploy.py archive-materials --purge-immediately
python deploy.py ocr-materials              # zip 会解压递归 OCR，LibreOffice 必装
python deploy.py sync
python deploy.py wiki --year 大二
python deploy.py docs --year 大二
python deploy.py link --year 大二

# 阶段 3：鲁棒性测试（用户在飞书 UI 表单提交主数据表里没有的新课）
python deploy.py archive-materials --purge-immediately
python deploy.py ocr-materials
python deploy.py sync                        # 应跳过 + 警告
# 用户在 bitable UI 课程主数据表加这门课
python deploy.py sync                        # 这次走通
python deploy.py docs --year 大一
python deploy.py link --year 大一
```

## 下次 session 推进优先级

1. **用户跑实测**：按上面流程，从阶段 0 开始。可能遇到的坑：
   - setup.bat 中文乱码（Windows cmd 默认 GBK）
   - winget 权限或网络问题（LibreOffice 装失败）
   - contact API 权限（grant-wiki 转 openid 时飞书应用要有 contact 权限）
   - open-wiki 对 wiki 节点是否生效（飞书 API 实测）
   - zip 解压 OCR（概率论前五章ppt.zip）
   - LLM 摘要质量（OCR 全文截断到 40000 字符问题）
2. **根据实测反馈修 bug**
3. **下一阶段目标**（已记 production-checklist 2.1i/j/k）：
   - 摘要 map-reduce 分块 + 长度收紧 ≤1000 字
   - 相似度检测 + LLM 串讲合并 + 管理员合并命令
   - 枚举自定义（移到 yaml）
   - 项目架构整理 + 上云文件审视

## 关键发现 / 待办

- **实测启用时课程录入-20260619.xlsx 实际是 GBK 编码 TSV**（被错存为 .xlsx 扩展名）。代码已兼容（_read_course_table sniff 格式 + 编码）
- **概率论的「前五章ppt.zip」**：10MB zip 文件，OCR 时会解压递归处理（每个 PPT 转 PDF → OCR）。LibreOffice 必装
- **飞书表单单选字段不支持「其他+自由填写」**：学生填表选不到主数据表里没有的课。学生反馈新课时走管理员路径（双层门控）
- **课程主数据表「课程」字段不是单选**：是文本字段。学生表单的「课程」下拉是从资料表「课程」字段选项读，sync 不会自动同步主数据表课程名到资料表「课程」字段选项——**需要在飞书 UI 手动同步**或写 fix 命令
- **services 层 11 个失败测试**：82df9c4 docx 表格两层结构重构后没更新 test_blocks / test_docx_table。今日范围外
- **`replace_year_overview` 失败**：学年总论无法追加到学年文档。今日范围外
- **docs folder_token 共享目录**：docx 建在应用 owner「我的空间」根目录，用户在 UI 看不到孤儿。今日范围外
- **资料串讲 prompt 生硬**：LLM 输出不够连贯系统。今日范围外
- **飞书 wiki API 限制**：member_type 只接受 openid/userid/departmentid，不接受 email/mobile。已通过 contact API 转换解决
- **飞书 wiki 空间没有「凭链接可编辑」总开关**：只能对每个页面（docx）单独开。open-wiki 命令对每个学年文档单独调 open_doc_public

## 备注

- 计划详情：`C:\Users\a\.claude\plans\golden-bouncing-hare.md`
- 今日 commit 7 个（按时间）：
  - `f9c65cc` WP1-WP4 重构
  - `727152b` WP5 README + 5 篇系列文档
  - `e61f962` README + 00 文科生视角重写
  - `602aea2` 加 setup.bat / setup.sh
  - `e061bb0` 加 venv + start.bat
  - `c99cad4` Python 版本检测改进
  - `a219d6b` grant-wiki + Excel 枚举值说明 + 保存 URL
  - `982597e` grant-wiki 支持手机号 + open-wiki
- 工作树干净，所有 commit 已 push
- 用户独立完成：阿里云 OSS bucket ACL 改公共读 + 飞书 UI 操作
- 接下来等用户跑实测，按反馈调整
